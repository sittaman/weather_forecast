name: weather_forecast
description: Flutter study project
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter  
  cupertino_icons: ^1.0.2
  json_annotation: ^4.7.0
  json_serializable: ^6.5.4
  mockito: ^5.3.2
  equatable: ^2.0.5
  dio: ^4.0.6
  http_mock_adapter: ^0.3.3
  shared_preferences: ^2.0.18
  connectivity_plus: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3

flutter:  
  uses-material-design: true